version: "3"

services:

  app:
    env_file: ./.env
    build:
      context: .
    ports:
      - 0.0.0.0:8080:8080
    volumes:
      - gnames:/var/opt/gnames
    command:
      - rest
      - -p
      - "8080"
      - -d

volumes:
  gnmatcher:

